# Tweak to prevent 'Write failed: Broken pipe'
TCPKeepAlive no
ServerAliveInterval 15
ServerAliveCountMax 10

# Ensure keys are added to agent and passphrases are saved to keychain
AddKeysToAgent yes
UseKeychain yes

# Include local config
Include config.local
